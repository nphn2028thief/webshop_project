const routes = {
    home: '/',
    catalog: '/catalogs',
    product: '/catalogs/:slug',
    cart: '/cart',
    login: '/account/login',
    register: '/account/register',
};

export default routes;
